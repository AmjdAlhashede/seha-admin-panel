{
  "name": "seha-admin-panel",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "description": "Seha Admin Panel - Standalone",
  "scripts": {
    "dev": "node api/index.js",
    "server": "node api/index.js",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate --schema=./prisma/schema.prisma && prisma db push --accept-data-loss"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "prisma": "^5.0.0",
    "@prisma/client": "^5.0.0"
  }
}